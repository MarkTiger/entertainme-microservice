const router = require('express').Router();
const Controller = require('../controllers/Movies');

router.get('/');

router.post('/');

router.put('/:id');

router.delete('/:id');

module.exports = router;
